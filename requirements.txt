# --- Librerías principales de análisis de datos ---
pandas>=2.1.0
numpy>=1.26.0

# --- Librerías para exportar a Excel y crear gráficos ---
XlsxWriter>=3.1.0
openpyxl>=3.1.2

# --- Librerías para interfaz gráfica (si tu app usa tkinter o ttkbootstrap) ---
tk>=0.1.0
ttkbootstrap>=1.10.1

# --- Librerías auxiliares y visualización ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Librerías de sistema / compilación (si generas .exe) ---
pyinstaller>=6.0.0

# --- Manejo de fechas y utilitarios ---
python-dateutil>=2.9.0
pytz>=2023.3

# --- Opcional: para leer archivos CSV/Excel grandes más rápido ---
pyarrow>=14.0.1

# --- Si tu aplicación genera PDFs (opcional futuro) ---
reportlab>=4.0.6
